flutter_app:
  command: atomic-calendar
  arch: x64
  parent: /usr/local/lib
  nonInteractive: false

control:
  Package: atomic-calendar
  Version: 0.0.1
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer: Atomic Blend
  Description: End-to-end encrypted calendar application

options:
  exec_out_dir: debian/packages